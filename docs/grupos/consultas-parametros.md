# Consultas, parâmetros e desempenho (página desmembrada)

Esta página existia como um agrupamento único, mas foi separada para ficar mais clara:

- **Dados e Consultas**: ver `Dados e Consultas` no menu (inclui o **item 15**).
- **Parâmetros**: ver `Parâmetros` no menu (inclui o **item 24**).
- **Desempenho**: ver `Desempenho` no menu (inclui o **item 47**).

Se você chegou aqui por um link antigo, use o menu lateral para navegar para as novas páginas.

